---
type Props = {
    category: string
}

const {category} = Astro.props
---

<a
    href={`/category/${category}`}
    class="text-link hover:text-border-divider uppercase text-xs"
>
    {category}
</a>