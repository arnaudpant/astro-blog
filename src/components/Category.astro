---
type Props = {
    category: string
}

const {category} = Astro.props
---

<a
    href={`/category/${category}`}
    class="text-teal-900 rounded-3xl uppercase text-sm px-2 py-1 border-1 border-teal-900 whitespace-nowrap"
>
    {category}
</a>