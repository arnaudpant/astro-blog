---
import { getCopyWriting } from "../lib/utils";
import ReseauxSociaux from "./ReseauxSociaux.astro";
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";

const allBlogArticles: CollectionEntry<'posts'>[] = await getCollection("posts")
const copyWriteYear: string = getCopyWriting().toString()
console.log("test")
---

<footer class="flex flex-col md:flex-row justify-between items-center px-24 py-6 max-sm:px-5 max-sm:py-4 max-w-3xl w-full mx-auto bg-fond gap-2">
  <div>
    <p>Tous droits réservés - {copyWriteYear}</p>
  </div>
  <div class="flex gap-4 py-4">
       <ReseauxSociaux />
    </div>
</footer>